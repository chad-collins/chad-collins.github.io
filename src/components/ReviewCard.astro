---
import { Image } from 'astro:assets';
import ReviewStars from './ReviewStars.astro';
import { getTagColor } from '../utils/tags';

interface Props {
	title: string;
	reviewImage?: any;
	tags?: string[];
	rating?: number;
	creator?: string;
	releaseYear?: number;
	pubDate: Date;
	href?: string;
	isSticky?: boolean;
}

const { 
	title, 
	reviewImage, 
	tags = [], 
	rating, 	
	creator, 
	releaseYear, 
	pubDate,		
	href,
	isSticky = false 
} = Astro.props;

const reviewType = tags.find(tag => tag !== 'review') || tags[0];
const tagLightColor = reviewType ? getTagColor(reviewType, false) : '';
const tagDarkColor = reviewType ? getTagColor(reviewType, true) : '';
---

<div class={`review-card ${isSticky ? 'sticky' : ''}`} style={`--card-bg: color-mix(in srgb, ${tagLightColor} 8%, transparent); --card-bg-hover: color-mix(in srgb, ${tagLightColor} 16%, transparent); --tag-border: ${tagLightColor}; --tag-border-dark: ${tagDarkColor};`}>
	{reviewImage && href && (
		<a href={href} class="review-card__image-link">
			<div class="review-card__image">
				<Image 
					src={reviewImage} 
					alt={`Cover art for ${title}`}
					width={400}
					height={200}
					class="review-card__image-img"
				/>
			</div>
		</a>
	)}
	{reviewImage && !href && (
		<div class="review-card__image">
			<Image 
				src={reviewImage} 
				alt={`Cover art for ${title}`}
				width={400}
				height={200}
				class="review-card__image-img"
			/>
		</div>
	)}
	<div class="review-card__content">
		<div class="review-card__header">
			{href ? (
				<a href={href} class="review-card__title-link">
					<h3 class="review-card__title">{title}</h3>
				</a>
			) : (
				<h3 class="review-card__title">{title}</h3>
			)}
		</div>
		{creator && (
			<p class="review-card__creator">
				<small>
					{creator}
					{releaseYear && <span class="review-card__year"> • {releaseYear}</span>}
				</small>
			</p>
		)}
	</div>
	{rating !== undefined && (
		<div class="review-card__footer" style={`--tag-light: ${tagLightColor}; --tag-dark: ${tagDarkColor};`}>
			{reviewType && (
				<div class="review-card__tag-row">
					<div class="review-card__separator"></div>
					<small class="review-card__tag">{reviewType}</small>
					<div class="review-card__separator"></div>
				</div>
			)}
			<div class="review-card__rating">
				<ReviewStars rating={rating} lightColor={tagLightColor} darkColor={tagDarkColor} />
			</div>
		</div>
	)}
</div>

<style>
	/* Card container */
	.review-card {
		display: flex;
		flex-direction: column;
		min-height: 350px;
		text-decoration: none;
		color: inherit;
		padding: 1rem;
		margin: 0;
		background-color: none;
	}
	
	.review-card__image-link {
		display: block;
		text-decoration: none;
		width: fit-content;
		margin: 0 auto;
	}
	
	.review-card__image-link:hover .review-card__image {
		opacity: 0.85;
		transition: opacity 0.15s ease;
	}
	
	.review-card__title-link {
		text-decoration: none;
		color: inherit;
		width: fit-content;
		display: block;
		margin: 0 auto;
	}
	
	.review-card__title-link:hover .review-card__title {
		color: rgb(var(--tag-border);
	}
	
	:global(.dark) .review-card__title-link:hover .review-card__title {
		color: rgb(var(--tag-border-dark);
	}
	
	.review-card.sticky {
		position: sticky;
		top: 100px;
	}
	
	:global(.dark) .review-card {
		border-color: rgb(var(--tag-border-dark);
	}
	
	/* Image */
	.review-card__image {
		width: 100%;
		height: 200px;
		margin-bottom: 0.75rem;
		overflow: hidden;
	}
	
	.review-card__image-img {
		width: 100%;
		height: 100%;
		object-fit: contain;
		display: block;
	}
	
	/* Content */
	.review-card__content {
		display: flex;
		flex-direction: column;
		flex: 1;
		gap: 0.5rem;
	}
	
	.review-card__header {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}
	
	.review-card__title {
		font-size: 1em;
		font-weight: 600;
		margin: 0;
		line-height: 1.3;
		letter-spacing: -0.01em;
		text-align: center;
	}
	
	.review-card__tag {
		font-size: 0.6em;
		letter-spacing: 0.06em;
		font-weight: 500;
		color: rgb(var(--tag-light);
		margin: 0 0.5rem;
	}
	
	:global(.dark) .review-card__tag {
		color: rgb(var(--tag-dark);
	}
	
	.review-card__creator {
		font-size: 0.75em;
		color: rgb(var(--gray));
		margin: 0;
		line-height: 1.4;
		text-align: center;
	}
	
	.review-card__year {
		opacity: 0.7;
	}
	
	/* Rating */
	.review-card__rating {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
	}
	
	.review-card__footer {
		margin-top: auto;
		padding-top: 0.5rem;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}
	
	.review-card__tag-row {
		display: flex;
		align-items: center;
	}
	
	.review-card__separator {
		flex: 1;
		border-bottom: 1px solid rgb(var(--tag-light);
		
	}
	
	:global(.dark) .review-card__separator {
		border-color: rgb(var(--tag-dark);
	}
	

</style>